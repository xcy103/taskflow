server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: unknown paths (e.g. /boards/:id) serve index.html so
  # React Router handles them on refresh instead of nginx 404-ing.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
